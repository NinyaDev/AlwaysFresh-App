<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Base.Theme.AlwaysFresh" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/fresh_green</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorPrimaryContainer">@color/light_green</item>
        <item name="colorSecondary">@color/warning_orange</item>
        <item name="android:statusBarColor">@color/primary_dark_green</item>
        <item name="android:colorBackground">@color/background_light</item>
        <item name="colorSurface">@color/card_background</item>
        <item name="colorSurfaceContainer">@color/card_background</item>
    </style>

    <style name="Theme.AlwaysFresh" parent="Base.Theme.AlwaysFresh" />

    <!-- Circular FAB shape (Material3 defaults to rounded square) -->
    <style name="ShapeAppearanceOverlay.AlwaysFresh.FAB" parent="">
        <item name="cornerSize">50%</item>
    </style>

    <!-- Green active indicator for navigation -->
    <style name="Widget.AlwaysFresh.NavIndicator" parent="Widget.Material3.BottomNavigationView.ActiveIndicator">
        <item name="android:color">@color/light_green</item>
    </style>
</resources>
